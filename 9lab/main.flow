
import frac;
import structs;
import algorithm;
import polynom;
import ds/vector;


to_div1(root: ArExpr) -> ArDiv{
    switch(root: ArExpr) {
        ArDiv(lhs, rhs): {
            ArDiv(lhs, rhs)
        }
        default: {
            __arexpr_ratfunc__(root);
        }

    }
}


main(){
    str = getUrlParameter("expr");
    root = arexpr_simplify(s2arexpr(str));
    ar_result = arexpr_simplify(__arexpr_ratfunc__(root));
    result_rat = arexpr2ratfun(root);

    println("");
    println("input");
    println(str);
    println("output");
    println(ratfun2s(result_rat));
    println("");    
    
}
