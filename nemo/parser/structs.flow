import ds/tree;

export {
    //main sector
    Program(Defenitions, Body);
    Defenitions( )
    
    //operators stuff

    //int expressions
    IntExpr ::= Var, Int, Sum, Sub, Mul, Div, Mod;
    
    //integer operations
    Sum(lhs: IntExpr, rhs: IntExpr);   
    Sub(lhs: IntExpr, rhs: IntExpr);  
    Mul(lhs: IntExpr, rhs: IntExpr);  
    Div(lhs: IntExpr, rhs: IntExpr);  
    Mod(lhs: IntExpr, rhs: IntExpr);  

    
    //boolean operations
    And();
    Or();
    Xor();
    Impl();
    
    //bool expressions
    
    
    //variables stuff

    Var(name: string, type: Type)

    Int(value: int);
    Bool(value: bool);
    Array(value: [Type]);
}