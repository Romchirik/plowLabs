
import frac;
import structs;
import algorithm;

main(){
    str = getUrlParameter("expr");
    root = arexpr_simplify(s2arexpr(str));
    result = differentiate_arexpr(root, getUrlParameter("var"));
    println(arexpr2s(arexpr_simplify(result)));
}