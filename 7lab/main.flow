
import frac;
import structs;
import algorithm;

main(){
    str = getUrlParameter("expr");
    root = s2arexpr(str);
    result = evaluate_arexpr(root); 
    if(result.denominator == 1){
        println(i2s(result.numerator));
    } else {
        println(frac2s(result));
    }
    
}